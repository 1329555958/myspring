info:
  component: Config Server
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/1329555958/myspring
          searchPaths: config-repo

server:
  port: 8888
