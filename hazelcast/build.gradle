dependencies {
    compile 'com.hazelcast:hazelcast:3.7'
    compile 'com.hazelcast:hazelcast-client:3.7'
    compile 'org.jolokia:jolokia-core'
    compile 'com.hazelcast:spring-data-hazelcast:1.0'
    compile project(":util")
}